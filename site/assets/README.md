# Demo assets

Drop the screen recording of the live `/demo` run here as `demo.mp4` (or `demo.webm`).
Variant C (Recorded + annotated) auto-detects it, hides the placeholder, and drives the
timed callouts off the video timeline. Callout timings live in `CALLOUT_TIMES` in `../app.js`.
